[package]
name = "iced-sitter"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
iced = { version = "0.3", features = ["canvas", "debug"] }
tree-sitter = "0.20"

[build-dependencies]
cc = "*"
